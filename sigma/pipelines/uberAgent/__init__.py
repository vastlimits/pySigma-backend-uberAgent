from .uberAgent import uberAgent_pipeline
# TODO: add all pipelines that should be exposed to the user of your backend in the import statement above.

pipelines = {
    "uberAgent_pipeline": uberAgent_pipeline,   # TODO: adapt identifier to something approproiate
}